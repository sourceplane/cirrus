---
title: "About"
nav: true
---

This site runs on **Virga**, a baseline for websites with one owner and
many visitors. Everything you see is a Markdown file in a repository; the
few things that need a server — subscribing, upvoting, counting a view —
run on Cloudflare Workers.

There is no console. The owner writes, pushes, and runs a CLI.
